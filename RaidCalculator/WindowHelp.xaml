<Window x:Class="RaidCalculator.WindowHelp"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RaidCalculator"
        mc:Ignorable="d"
        Title="Производительность RAID-массивов" 
        Height="450" 
        Width="1000"
        MinHeight="450"
        MinWidth="1000"
        WindowStartupLocation="CenterOwner" Icon="icon.ico">
    <Grid Margin="10">
        <!--<Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Frame Margin="0,0,0,10"/>

        <Button Content="Назад"
                HorizontalAlignment="Left"
                Grid.Row="1"/>

        <Button Content="В начало"
                HorizontalAlignment="Center"
                Grid.Row="1"/>

        <Button Content="Далее"
                HorizontalAlignment="Right"
                Grid.Row="1"/>-->
        <WebBrowser x:Name="WebBrowserMain"/>
    </Grid>
</Window>
